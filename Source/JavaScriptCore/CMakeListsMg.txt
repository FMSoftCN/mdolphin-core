LIST(APPEND JavaScriptCore_SOURCES
    jit/ExecutableAllocatorFixedVMPool.cpp
    jit/ExecutableAllocator.cpp
    runtime/MarkStackPosix.cpp
    wtf/OSAllocatorPosix.cpp
)

IF (ENABLE_VIDEO)
    LIST(APPEND JavaScriptCore_SOURCES
        wtf/gobject/GOwnPtr.cpp 
        wtf/gobject/GRefPtr.cpp
    )
ENDIF (ENABLE_VIDEO)

SET(JS_INSTALL_DIR "include/${CMAKE_PROJECT_NAME}")
INSTALL (FILES  "${JAVASCRIPTCORE_DIR}/API/JSBase.h" DESTINATION ${JS_INSTALL_DIR})
INSTALL (FILES  "${JAVASCRIPTCORE_DIR}/API/JSContextRef.h" DESTINATION ${JS_INSTALL_DIR})
INSTALL (FILES  "${JAVASCRIPTCORE_DIR}/API/JSObjectRef.h" DESTINATION ${JS_INSTALL_DIR})
INSTALL (FILES  "${JAVASCRIPTCORE_DIR}/API/JSStringRef.h" DESTINATION ${JS_INSTALL_DIR})
INSTALL (FILES  "${JAVASCRIPTCORE_DIR}/API/JSValueRef.h" DESTINATION ${JS_INSTALL_DIR})
INSTALL (FILES  "${JAVASCRIPTCORE_DIR}/API/WebKitAvailability.h" DESTINATION ${JS_INSTALL_DIR})
